package lol.ezra.model.fixture

class Universe {
   /**
    * Mapping of actual dmx address to internal patched address
    * DMX address (valid  from 1 - 512) to fixture address.
    */
   val fixtures = mutableMapOf<Int, Int>()
   fun assignFixture() {

   }

}